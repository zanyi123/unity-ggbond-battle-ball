# 2026-09-07 工作日志 — 迁移文档全面刷新日

## 今日目标

- [x] Skills 导入：.trae/skills 8 个 → 项目级 WorkBuddy skills（MD5 校验一致）
- [x] 迁移文档（`Docs/迁移方案.md`）全面刷新至代码实况（trae/Cursor 推进的进度此前未同步）
- [x] 新增 §13 剩余任务分块执行计划（A-J 十块，每块 MCP 预算封顶）
- [ ] 块 A（备战界面功能对表）— 未开始，下次入口

---

## 完成内容

### 1. 实况核查（纯本地，零 MCP）

**范围**：全 54 个 .cs 脚本清单核对 + 相机/动画/障碍/幻象/击退/timeScale 定向 grep。

**关键结论**：
- 文档严重滞后于代码：元灵 7 脚本（SpiritTagEffectHandler **70.9KB**）、备战 UI（PreparationUI **99.5KB**）、Physics 层 11 脚本（障碍/幻象/场地区域/击退）、AI 4 脚本共 **103.5KB**（含 SpiritAIManager 68.4KB）均已落地，此前文档全标"未做"。
- **纠错**：首轮把 grep 命中 LoadScene 误读为"暂停已实现"——实为全工程 **0 处 timeScale**，暂停未实现，已修正文档 3 处错误结论。

### 2. 文档刷新（`Docs/迁移方案.md`）

- §5.5–5.10（元灵/Buff）：状态全部翻正 ✅，附落地链路说明
- §6 六节（UI 层）：整体 85%，结算装备对比为唯一 ❌（块 D）
- §7 七节（战斗层）：75%，**BattleHud 仅 2.6KB 骨架**（文件头自述 `[TODO Step-*]`，块 C）
- §8 信号→event：18 文件 `event Action` ✅；§9/§10/§11 资源/优先级/完成度总览按实况重估
- §12 新增 6 条 Unity 实战原则（3D 物理禁混 2D、真调用优先事件兜底、骨架先行、GD 兼容层收口在 Player、比例尺 0.01 铁律等）
- §13 新增：A-J 分块计划 + 执行纪律（预算封顶、三同步、危险项 0 容忍）
- 修复 5.2 乱码 1 处；结构自检 51 标题无断层、乱码 0 残留

### 3. 配套勾验

`MIGRATION_CHECKLIST.md` A1 已勾：**54 .cs 编译 0 错 0 警**（实际数量超清单所写 22，以 MCP 核查为准）。

---

## 验证指引（主人 Unity 端抽查建议）

按 CHECKLIST 危险项优先，30 分钟内可完成的抽查：

| 优先 | 检查项 | 预期 |
|---|---|---|
| 🔴 | 打开 Unity 等编译，看 Console | 0 Error 0 Warning |
| 🔴 | MainMenu → 备战 → 开始比赛 → 打完一场 → 结算 → 返回主菜单 | 全链路无 NRE、比分能动 |
| 🟡 | 备战界面：换球员/换元灵/换策略后开赛 | 设置生效、AI 行为随策略变化 |
| 🟡 | 结算页三币种奖励 + 连胜显示 | 与存档一致 |
| ⚪ | 场上障碍/幻象是否随机出现 | 出现且碰撞生效 |

已知预期缺口（非 bug，勿误报）：**战斗无 HUD 显示、比赛不可暂停、结算无装备对比、相机单一视角、球员无动画**——均已在 §13 排期。

---

---

## 晚间批次（验收返修 → 主流程打通 → HUD 落实 → 白线/技能链路）

### 4. 验收不合格返修（3 文件，编译零错误）

| # | 缺陷 | 文件 | 修复 |
|---|---|---|---|
| 1 | 开始比赛后备战界面不出现 | `Battle/BattleManager.cs` | 竞态：Start() 发事件时 BattleUIBridge 尚未订阅 → `_prepRequested` 标志 + 首帧 Update 再发 |
| 2 | 元灵面板空白 | `UI/SpiritUI.cs` | LoadData() 空列表兜底回退测试数据 + LogWarning |
| 3 | 基地装备/营养列表不显示 | `UI/BaseSystem.cs` | Grid 误挂 ScrollView 本体 → 改挂 `ScrollRect.content` |

### 5. 角色系统 UI 排版失效修复

**文件**：`UI/CharacterSystem.cs`
**改动**：移除右侧 Content 的 VerticalLayoutGroup/ContentSizeFitter（其覆盖子元素手动定位导致"数据在、排版全无"），改纯 RectTransform 手动定高，子元素绝对定位。
**验证**：Play Mode 实测各子元素位置/宽度比例（体力 80/100 → 填充 280/350）全部正确。

### 6. 主流程全链路自测 + 备战界面真正根因修复

**根因**：`RuntimeInitializeOnLoadMethod` 仅应用启动时触发一次，LoadScene 进 TestBattle 后不再触发 → BattleUIBridge 永不创建。
**修复**：`UI/BattleUIBridge.cs` 增加 `sceneLoaded` 兜底创建。
**实测**：模式选择→主菜单→开始比赛→备战界面（35 节点）→正式比赛→中场→结算（0:3）→返回主菜单，全链路 ✅。
**排误**：编辑器后台运行导致播放器循环近乎冻结（模拟点击/场景加载异常均为环境现象非 Bug）。

### 7. 备战界面偏移修复 + BattleHud 落实

- **PreparationUI 偏移**：`UI/PreparationUI.cs` 6 处——Start() 补 `SetFullRect`（全工程唯一漏调者）+ 4 卡片构建器 + FoodOption 统一左上锚定；实测 12 卡片全部落画布内 ✅
- **BattleHud 重写**：`Battle/BattleHud.cs`——顶部比分/时间栏 + 底部己方球员卡（HP/体力/持球/阵亡）；坑：`team_a_players` 从不填充，必须走 `GetTeamAPlayers()`；asmdef 补 UnityEngine.UI 引用

### 8. 场地白线规则 + BattleHud 字段修正 + 元灵技能链路打通

- **白线对齐**（内场±7.6/±5.2、外场±10.2/±6.5）：PlayerController/BallController 代码默认值 + TestBattle.unity 序列化值（12+6 处 PowerShell 替换）+ 出生点±11→±9。坑：场景序列化值覆盖代码默认，必须同步改 .unity。
- **BattleHud 字段按 GD 原版重构**：绿 HP（体力即血量）+ 蓝元灵能量条 + 3 个技能冷却框（遮罩自顶向下按 cd/maxCd 覆盖 + 倒计时）。
- **技能链路 3 连环 Bug**：① EquipSpirit 是 stub → 落实写入（技能表在 spirits.json 的 skills 字段，非 skills.json 反查）；② SpiritSkillTrigger 只缓存通用技能 → 合并 GetAllSpiritSkills()；③ JArray/JObject 嵌套 as 转换恒 null → 显式类型转换。
- **实测全通过**：clamp/出生点生效、EquipSpirit→注册、use_skill=True 冷却 9.5s、HUD 绿 HP/蓝灵能/技能框遮罩比例正确。

---

## 待办 / 遗留

- [ ] 主人前台复验：比赛内 HUD 实机视觉（技能冷却动态下降）、球员活动范围贴合白线观感
- [ ] 技能释放玩家侧输入入口 + 能量实际扣减（_consume_energy 恒 true 是 TODO）未接
- [ ] FieldZoneManager 旧值未×2 且场景无该对象，SpiritTagEffectHandler 找不到时会报错——未处理
- [ ] 块 A（备战对表，0 MCP）→ B（事件审计，0 MCP）→ D（结算装备对比）→ G(B层) → E（多相机）→ F（球员动画）
- [x] 本地 16 commits + 本批修改 → 推送 origin（unity-ggbond-battle-ball）
